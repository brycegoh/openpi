# --- Secrets (loaded from .env file automatically) ---
# Add these to .env in the project root:
#   RUNPOD_API_KEY=...
#   HF_TOKEN=...
#   WANDB_API_KEY=...

# --- RunPod Pod Settings ---
pod_name: "openpi-training"
template_id: "8u8g3zo2jg"
gpu_type: "NVIDIA A100-SXM4-80GB"
gpu_count: 4
cloud_type: "SECURE"
container_disk_gb: 50
volume_gb: 500
volume_mount_path: "/workspace"

# --- Git Settings ---
git_repo: "https://github.com/brycegoh/openpi.git"
git_branch: "v2.1"

# --- Training Settings ---
dataset_repo: "griffinlabs/B017_dataset"
base_model_repo: "griffinlabs/pi05_1877ep_pytorch"
base_model_repo_path: "pi05_tcr_full_finetune_pytorch/pi05_1877ep/57000"   # optional: subfolder within repo, e.g. "pi05_tcr_full_finetune_pytorch/pi05_1877ep/54000"
exp_name: "pi05_B017"
batch_size: 384
num_train_steps: 30000
save_interval: 877         # checkpoint every N steps
keep_period: 877            # keep checkpoints where step % keep_period == 0
base_checkpoint: "/workspace/base_checkpoints/pi05_base_pytorch"
checkpoint_base_dir: "/workspace/checkpoints"
wandb_enabled: true
num_gpus: 4                  # nproc_per_node for torchrun

# --- Optional ---
stop_after_training: false   # auto-terminate pod when training finishes
